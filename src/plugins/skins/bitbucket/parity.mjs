/**
 * The Bitbucket skin's reviewed target vocabulary, beside the skin it describes.
 *
 * The words Bitbucket's own project and profile menus carry — what
 * `style-parity` checks the applied skin's menu against. The navigation *shape*
 * is not here; it comes from the skin's `layout`. Node-only.
 */
export default {
  vocab: {
    project: ['Source', 'Pull requests', 'Pipelines'],
    profile: ['Overview', 'Repositories', 'Projects', 'Snippets'],
  },
};
