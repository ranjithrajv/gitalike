dist/
node_modules/

# Scratch output from the Playwright MCP tool and any Playwright runs.
.playwright-mcp/
test-results/
playwright-report/

# The WebP variants of the docs/ captures are generated at deploy time by
# .github/workflows/pages.yml, not committed; only the PNGs are tracked.
docs/*.webp
